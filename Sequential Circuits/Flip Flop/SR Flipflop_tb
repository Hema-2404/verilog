module tb_sr_ff;
    reg S, R, clk;
    wire Q;

    sr_ff uut (.S(S), .R(R), .clk(clk), .Q(Q));

    always #5 clk = ~clk;

    initial begin
        $dumpfile("sr_ff.vcd");
        $dumpvars(0, tb_sr_ff);

        clk=0;
        S=0; R=0; #10;
        S=1; R=0; #10;
        S=0; R=1; #10;
        S=1; R=1; #10;

        $finish;
    end
endmodule
